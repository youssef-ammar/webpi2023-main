<form method="post" action="{{ path('app_freelancer_delete', {'idMat': formation.idMat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.idMat) }}">
    <button class="btn">Delete</button>
</form>

