<form method="POST" action="{{ path('app_formation_delete', {'idMat': formation.idMat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.idMat) }}">
    <button class=""btn btn-danger">Delete</button>
</form>

