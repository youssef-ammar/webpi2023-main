<!DOCTYPE html>

<title>Edit TypeEvenement</title>

{% block body %}
    <h1>Edit TypeEvenement</h1>

    {{ include('type_evenement/new.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_type_evenement_index') }}">back to list</a>

    {{ include('type_evenement/_delete_form.html.twig') }}
{% endblock %}
