{% extends 'base.html.twig' %}

{% block title %}Inscription index{% endblock %}

{% block body %}
    <h1>Inscription index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idinscri</th>
                <th>Disponibilite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for inscription in inscriptions %}
            <tr>
                <td>{{ inscription.idinscri }}</td>
                <td>{{ inscription.disponibilite }}</td>
                <td>
                    <a href="{{ path('app_inscription_show', {'idinscri': inscription.idinscri}) }}">show</a>
                    <a href="{{ path('app_inscription_edit', {'idinscri': inscription.idinscri}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_inscription_new') }}">Create new</a>
{% endblock %}
